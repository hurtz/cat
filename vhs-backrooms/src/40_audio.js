/* ============================================================================
   AUDIO — placeholder.
   ========================================================================== */
VB.def('audio', function (VB, THREE) {
  return {
    init() { VB.audio = { ctx: null, master: null, unlock() {}, sfx() {} }; },
  };
}, 40);
